name: FTP Minimal Test

on:
  workflow_dispatch:

jobs:
  ftp-test:
    runs-on: ubuntu-latest

    steps:
      - name: FTP connect test
        run: |
          curl -v \
            --ftp-pasv \
            -u "gitftp@acewp.ir:J47iShIj0EpyuJ95" \
            "ftp://server77s.irwebspace.com/"
