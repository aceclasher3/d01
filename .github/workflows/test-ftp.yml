name: PHP Upload Test

on:
  workflow_dispatch:

jobs:
  upload:
    runs-on: ubuntu-latest

    steps:
      - name: Create test file
        run: |
          echo "Hello from GitHub Actions 🚀" > test.txt
          date >> test.txt

      - name: Simple HTTP test
        run: |
          curl -v https://acewp.ir/dev/uploader.php
